<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageViewer"
        xmlns:userControls="clr-namespace:ImageViewer.MVVM.View.userControls"
        
        xmlns:emguConverter="clr-namespace:ImageViewer.MVVM"
        
        mc:Ignorable="d"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
         TextElement.Foreground="{DynamicResource MaterialDesignBody}"
         TextElement.FontWeight="Regular"
         TextElement.FontSize="13"
         TextOptions.TextFormattingMode="Ideal"
         TextOptions.TextRenderingMode="Auto"
         Background="#FFFAF3"
         FontFamily="{DynamicResource MaterialDesignFont}"
        Title="MainWindow" Height="450" Width="800">
    
    <Grid >
        
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="80*"/>
            <RowDefinition Height="17*"/>
        </Grid.RowDefinitions>
        
        <userControls:menuBar Grid.Row="0" VerticalAlignment="Top"/>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="40*"/>
                <ColumnDefinition Width="40*"/>
                <ColumnDefinition Width="20*"/>
            </Grid.ColumnDefinitions>

            <Image Grid.Column="0" Source="{Binding DisplayImage.Path, UpdateSourceTrigger=PropertyChanged}"
               Margin=" 10 10 10 10" HorizontalAlignment="Center" VerticalAlignment="Center"/>

            <!--<Image Grid.Column="1" 
               Margin=" 10 10 10 10" HorizontalAlignment="Center" Source="{Binding FilterImage, Converter={emguConverter:Converter}}" VerticalAlignment="Center"/>-->

            <Image Grid.Column="1" Margin=" 10 10 10 10" HorizontalAlignment="Center" VerticalAlignment="Center">
                <Image.Style>
                    <Style TargetType="Image">
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding ImageColor}" Value="True">
                                <Setter Property="Source" Value="{Binding BgrImage, Converter={emguConverter:Converter}}"/>
                            </DataTrigger>
                            <DataTrigger Binding="{Binding ImageColor}" Value="False">
                                <Setter Property="Source" Value="{Binding GrayImage, Converter={emguConverter:Converter}}"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </Image.Style>
            </Image>


            <Grid Grid.Column="2">
                <StackPanel>
                    <Expander  HorizontalAlignment="Stretch" Header="Blur">
                        <StackPanel
                            Margin="24,8,24,16"
                            Orientation="Vertical"
                            TextBlock.Foreground="{DynamicResource MaterialDesignBody}">


                            <Slider Minimum="0" Maximum="30" Value="{Binding BlurValue, Mode=TwoWay}" />
                            
                        </StackPanel>
                    </Expander>
                    <Border />
                    <Expander  HorizontalAlignment="Stretch" Header="Treshold">
                        <StackPanel
                            Margin="24,8,24,16"
                            Orientation="Vertical"
                            TextBlock.Foreground="{DynamicResource MaterialDesignBody}">

                            <Slider Minimum="0" Maximum="255"  Value="{Binding TreshValue, Mode=TwoWay}"/>


                            <Slider Minimum="0" Maximum="255"  Value="{Binding MaxTreshValue, Mode=TwoWay}"/>

                            <ComboBox ItemsSource="{Binding ThresholdType}"
                                >
                            </ComboBox>

                        </StackPanel>
                    </Expander>
                    <Border />
                    <Expander  HorizontalAlignment="Stretch" Header="Eroding">
                        <StackPanel
                            Margin="24,8,24,16"
                            Orientation="Vertical"
                            TextBlock.Foreground="{DynamicResource MaterialDesignBody}">


                            <TextBlock Text="Erode Iterations:"/>
                            <Slider Value="{Binding ErodeIterations, Mode=TwoWay}" Minimum="1" Maximum="10" TickFrequency="1" TickPlacement="BottomRight"/>

                            <TextBlock Text="Dilate Iterations:"/>
                            <Slider Value="{Binding DilateIterations, Mode=TwoWay}" Minimum="1" Maximum="10" TickFrequency="1" TickPlacement="BottomRight"/>
                            

                        </StackPanel>
                    </Expander>
                    <Border />
                    <Expander  HorizontalAlignment="Stretch" Header="Edge ">
                        <StackPanel
                            Margin="24,8,24,16"
                            Orientation="Vertical"
                            TextBlock.Foreground="{DynamicResource MaterialDesignBody}">
                            <Button Content="Toggle Image Color" Command="{Binding ToggleColor}"/>

                            <Slider Minimum="0" Maximum="255"  Value="{Binding EdgeTresh1, Mode=TwoWay}"/>

                            <Slider Minimum="0" Maximum="255"  Value="{Binding EdgeTresh2, Mode=TwoWay}"/>

                        </StackPanel>
                    </Expander>
                    <Border />
                </StackPanel>

            </Grid>
            

        </Grid>
        <userControls:customListView Grid.Row="2" HorizontalAlignment="Center"  />
    </Grid>
</Window>
