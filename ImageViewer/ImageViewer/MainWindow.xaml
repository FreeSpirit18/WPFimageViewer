<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ImageViewer"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="20*"/>
            <ColumnDefinition Width="80*"/>
        </Grid.ColumnDefinitions>
        <Grid Grid.Column="0">
            <Grid.RowDefinitions>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="10*"/>
                <RowDefinition Height="80*"/>
            </Grid.RowDefinitions>

            <Button Grid.Row="0" Width="100" Height="20" VerticalAlignment="center" HorizontalAlignment="Center"
                    Content="Select image" Click="Select_Click"></Button>
            <Button Grid.Row="1" Width="100" Height="20" VerticalAlignment="center"
                    HorizontalAlignment="Center"  Content="Clear" Click="Clear_Click"></Button>
            <ListView Name="folderImages" Grid.Row="2" ItemsSource="{Binding FolderImages}" SelectionMode="Single" SelectionChanged="folderImages_SelectionChanged"/>
        </Grid>
        <Image Grid.Column="1" Source="{Binding Display, UpdateSourceTrigger=PropertyChanged}"
               Margin=" 10 10 10 10"/>

    </Grid>
</Window>
